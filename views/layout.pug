doctype html
html
  head
    title= title
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")

    link(rel='stylesheet', href='/stylesheets/bulma.min.css')
    script(defer src='https://use.fontawesome.com/releases/v5.3.1/js/all.js')

  body
    nav(class="navbar is-dark", role="navigation" aria-label="main navigation")
      div(class="navbar-brand")
        a(class="navbar-item", href="/") Localization Simple Tool
      div(class="navbar-menu")  
        div(class="navbar-start")  
          a(href="/", class="navbar-item") Home

        div(class="navbar-end")  
          div(class="navbar-item")  
            div(class="buttons")  
              if session && session.user
                a(href="/users/logout", class="button is-light") Logout
              else
                a(href="/users/create", class="button is-primary") 
                  strong Sign Up
                a(href="/users/login", class="button is-light") Login
    
    div(class="container is-widescreen")
      br
      br 
      block content
